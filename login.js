const invite=document.getelement("invite");
const admin=document.getelement("admin");
const login=document.getelement("login");
const pw=document.getelement("pw");
document.getElementById('btn-login').addEventListener('click',()=>
    document.querySelectorAll(".error").forEach(e)=> e .textcontent="");
    let test=false;}

    if (test)
{
    if(invite.checked && (pw.value)=user)
}


